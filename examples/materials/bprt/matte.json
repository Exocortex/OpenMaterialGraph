matte:
   diffuse: color
   sigma: float // oren-nayar roughness control, guassian sigma
   normal: normal


if( sigma === 0 )
	bsdf.add( new LambertianReflection( diffuse ) );
else
	bsdf.add( new OrenNayar( diffuse ) );	
