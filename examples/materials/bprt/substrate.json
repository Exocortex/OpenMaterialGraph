substrate:
	diffuse: color
	specular: color
	uRoughness: float
	vRoughness: float
	normal: normal



if( diffuse !== BLACK || specular !== BLACK )
	var microfacetDistribution = new TrowbridgeReitzDistribution( uRoughness, vRoughness );
	 bsdfs.add( new FresnelBlend( diffuse, specular, microfacetDistribution ) );

///////////////////////////////////////  Optimized

var microfacetDistribution = new TrowbridgeReitzDistribution( uRoughness, vRoughness );
bsdfs.add( new FresnelBlend( diffuse, specular, microfacetDistribution ) );
