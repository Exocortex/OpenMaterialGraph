mirror:
  reflectance: color
  normal: normal


if( reflectance !== BLACK )
	var fresnel = new FresnelNoOp();
	bsdfs.add( new SpecularReflection( reflectance, fresnel ) );


///////////////////////////////////////  Optimized

var fresnel = new FresnelNoOp();
bsdfs.add( new SpecularReflection( reflectance, fresnel ) );
