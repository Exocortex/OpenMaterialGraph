/**
 * @author bhouston / http://clara.io
 *
 * A Node with a BSDF output.  Or derived from the BSDF Node type.
 *
 */

OMG.DIRECTION = {
	IN,
	OUT
};

OMG.DATA_TYPES = {
	FLOAT,
	COLOR,
	POSITION,
	NORMAL,
	UV,
	INTEGER,
	BOOLEAN,
	TEXTURE,
	BSDF,
};
